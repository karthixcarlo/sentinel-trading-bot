"""Throwaway fixture file — not real code, see ../README.md."""

# Hardcoded fallback secret — intentional weak-default pattern, disposable fixture.
SECRET_KEY = "change-me-please-fake-default-secret"
DEBUG_MODE_ALWAYS_ON = True
JWT_ALGORITHM = "none"  # accepting an unsigned JWT — intentional fixture pattern
