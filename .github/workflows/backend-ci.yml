name: Backend CI

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - "agents/**"
      - "services/**"
      - "agent_service.py"
      - "sentinel_hive.py"
      - "sentinel_state.py"
      - "broker_engine.py"
      - "requirements.txt"
      - ".github/workflows/backend-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "backend/**"
      - "agents/**"
      - "services/**"
      - "agent_service.py"
      - "sentinel_hive.py"
      - "sentinel_state.py"
      - "broker_engine.py"
      - "requirements.txt"
      - ".github/workflows/backend-ci.yml"

jobs:
  lint:
    name: Lint (Ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Ruff
        run: pip install ruff

      - name: Ruff check (lint)
        run: ruff check backend/ agents/ services/ agent_service.py sentinel_hive.py sentinel_state.py broker_engine.py --select E,F,W --ignore E501,E402,W293,W291

      - name: Ruff format check
        run: ruff format --check backend/ agents/ services/ agent_service.py sentinel_hive.py sentinel_state.py broker_engine.py || true

  compile:
    name: Compile Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Compile all Python files
        run: |
          python -m py_compile backend/main.py
          python -m py_compile agent_service.py
          python -m py_compile sentinel_hive.py
          python -m py_compile sentinel_state.py
          python -m py_compile broker_engine.py
          python -m py_compile agents/supervisor.py
          python -m py_compile agents/scout_agent.py
          python -m py_compile agents/analyst_agent.py
          python -m py_compile agents/risk_manager.py
          python -m py_compile agents/trader_agent.py
          python -m py_compile services/auth_manager.py
          python -m py_compile services/news_loader.py

  test:
    name: Tests (pytest)
    runs-on: ubuntu-latest
    needs: compile
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-asyncio httpx

      - name: Run tests
        run: pytest tests/ -v --tb=short
        env:
          TESTING: "1"
